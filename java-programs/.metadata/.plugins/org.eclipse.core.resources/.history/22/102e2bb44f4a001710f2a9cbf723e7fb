public class OrderList {
	private int[] lista;
	
	OrderList(int tam) {
		lista = new int[tam];
	}
	
	public void bubblesort() {
		int[] sortedArray = Bubblesort.sort(lista);
		
		printArray(sortedArray);
	}
	
	public void printArray(int[] array) {
		for (int i = 0; i < array.length; i++) {
			System.out.println("array[" + i + "] = " + array[i]);
		}
	}
}
